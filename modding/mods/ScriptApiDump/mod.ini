[mod]
name = Script API Dump
author = tetration
version = 0.1
description = Developer tool: scans the game image for script function bindings (name -> address) and writes them to mods\wml.log at startup. Entries marked >>> are the ones the Saintify mod needs.
